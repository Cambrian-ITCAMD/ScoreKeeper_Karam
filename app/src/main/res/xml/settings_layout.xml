<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:iconSpaceReserved="true"
        android:key="switch_preference_1"
        android:title="Sava Data"
        android:checked="true"
        android:summary="Store data when the user exit the app."/>
    <ListPreference
        android:defaultValue="1"
        android:entries="@array/default_entries"
        android:entryValues="@array/default_values"
        android:key="list_preference_1"
        android:title="Score preference"
        android:summary="Gives choice for initial/starting scores."/>
</PreferenceScreen>